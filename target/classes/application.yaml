spring:
  jackson:
    time-zone: America/Mexico_City
  datasource:
    url: jdbc:postgresql://localhost:5432/opticabd?serverTimezone=America/Mexico_City
    username: postgres
    password: w1Ra|\y5|%!3j_IL
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


